# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/batuta-crm

# JWT Secret
JWT_SECRET=batuta-jwt-secret-key

# Frontend URL (for password reset emails, etc.)
FRONTEND_URL=http://localhost:5173

# Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASS=password

# Admin Email (for notifications)
ADMIN_EMAIL=admin@batuta.fr